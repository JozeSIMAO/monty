#include "monty.h"
/**
 * nop - does nothing
 * @stack: pointer to top nod in the stack
 * @line_num: line we're reading
 */
void nop(stack_t **stack, unsigned int line_num)
{
    (void)stack;
    (void)line_num;
}
